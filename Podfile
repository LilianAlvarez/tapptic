# Uncomment the next line to define a global platform for your project
 platform :ios, '8.0'

target 'Tapptic' do
    #Pod for Tapptic
    pod 'SDWebImage'
    # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
    use_frameworks!
    
    # Pods for Tapptic
    
    target 'TappticTests' do
        inherit! :search_paths
        # Pods for testing
    end
    
    target 'TappticUITests' do
        inherit! :search_paths
        # Pods for testing
    end
    
end
